// import { NextFunction, Request, Response } from "express";
import BasicOprtationsInterface from "../Basic/BasicOperationsInterface";

export default interface ChatInterface extends BasicOprtationsInterface {

    // createChat(project: object): Promise<void>;

    // createModel(req: Request, res: Response): Promise<object>;

}
